# Deployment Guide

[`docs/deployment.md`](docs/deployment.md) is the authoritative production
deployment runbook for all clients.

Use that runbook for GitHub/Cloudflare configuration ownership, automatic
releases, retry and rollback procedures, emergency recovery, and first-release
verification. Do not copy a client environment file over `.env`.
