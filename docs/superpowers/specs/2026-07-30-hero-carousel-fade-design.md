# Hero carousel fade transition

Use a 500ms opacity-only fade when the active Hero slide changes. Keep the current controls, auto-slide timing, and image sizing. Disable the transition for users who request reduced motion.
